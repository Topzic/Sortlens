# Sortlens v0.6.4

## Fixes And Polish

- Fixed the Windows release build blocking on strict TypeScript checks in the Duplicates and Settings pages.
- Rebuilt and verified the packaging pipeline end to end for the current desktop build.
- Aligned the Blurry and Duplicates page headers and empty-state spacing so those views match visually.

## Packaging

- Refreshed the Windows installer and zipped installer for the current codebase.
- No new workflow changes were introduced in this patch beyond build stability and UI consistency updates.
